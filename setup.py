from setuptools import find_packages, setup

setup(
    name='stone',
    packages=find_packages(include=["stone"]),
    version='0.1.0',
    description='Text formatting, coloring and tables library',
    keywords=["color", "cli", "table", "terminal-colors", "ansi", "rgb"],
    author='aniko33',
)
